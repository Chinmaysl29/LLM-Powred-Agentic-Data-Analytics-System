# ==========================================================
# Core Web Framework
# ==========================================================
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
pydantic-settings>=2.5,<3.0
websockets>=10.0

# ==========================================================
# Database & ORM
# ==========================================================
psycopg[binary]>=3.2,<4.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
sqlparse>=0.5,<1.0
pypdf>=4.0,<6.0
pymupdf>=1.24,<2.0

# ==========================================================
# Redis
# ==========================================================
redis>=5.0,<6.0

# ==========================================================
# ChromaDB Vector Database
# ==========================================================
chromadb>=0.5,<1.0

# ==========================================================
# LLM Providers
# ==========================================================
groq>=0.9,<1.0
google-generativeai>=0.8,<1.0
openai>=1.40,<2.0

# ==========================================================
# LangChain Ecosystem
# ==========================================================
langchain-core>=0.3,<1.0
langchain>=0.3,<1.0
langchain-groq>=0.2,<1.0
langchain-google-genai>=2.0,<3.0
langchain-openai>=0.2,<1.0
langchain-community>=0.3,<1.0
langgraph>=0.2,<1.0
langsmith>=0.1,<1.0

# ==========================================================
# Embeddings
# ==========================================================
# Embeddings
# ===========================================================
# Pin torch to the CPU-only wheel. This prevents pip from pulling
# multi-GB CUDA packages (nvidia_nccl_cu12, etc.) when resolving
# sentence-transformers. The +cpu build is fetched from the
# PyTorch CPU index (set via --extra-index-url in Dockerfile).
torch==2.5.1+cpu
sentence-transformers>=3.0,<4.0

# ==========================================================
# Data Processing
# ==========================================================
pandas>=2.2,<3.0
numpy>=2.0,<3.0
openpyxl>=3.1,<4.0
pyarrow

# ==========================================================
# Forecasting & ML
# ==========================================================
prophet>=1.1,<2.0
statsmodels>=0.14,<1.0
pmdarima>=2.0,<3.0
xgboost>=2.0,<3.0
scikit-learn>=1.5,<2.0

# ==========================================================
# Visualization
# ==========================================================
plotly>=6.0,<7.0
kaleido>=0.2,<1.0

# ==========================================================
# Monitoring
# ==========================================================
prometheus-client>=0.21,<1.0

# ==========================================================
# Report Generation
# ==========================================================
python-docx>=1.1,<2.0
reportlab>=4.2,<5.0
python-pptx>=1.0,<2.0
xlsxwriter>=3.2,<4.0

# ==========================================================
# Financial Data APIs
# ==========================================================
alpha-vantage>=3.0,<4.0
finnhub-python>=2.4,<3.0

# ==========================================================
# Authentication
# ==========================================================
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0

# ==========================================================
# Async & HTTP
# ==========================================================
aiohttp>=3.10,<4.0
aiofiles>=24.0,<25.0

# ==========================================================
# Testing
# ==========================================================
pytest>=8.0,<10.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0
